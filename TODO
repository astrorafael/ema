1) Upload measuements to an external server (i.e. Meteoclimatic) 
using an API (using requests or grequest). 
It should be designed as a nonbocking I/O not to disrupt server heartbeat.
Maybe a separate process o thread using a queue (ZeroMQ?)

